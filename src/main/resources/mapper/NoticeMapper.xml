<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.bit.ex.mapper.NoticeMapper">
    <select id="getList" resultType="edu.bit.ex.vo.NoticeVO">
        <![CDATA[
        select board_id, b_title, b_content, board_type_id, b_date from barny_board where board_type_id = 0 order by board_id desc
        ]]>
    </select>

</mapper>